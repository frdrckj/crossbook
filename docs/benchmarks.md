# Benchmarks

> Stub — populated at M1 (matching core) with `criterion` results.

Report, with full methodology (machine, input distribution, warmup):

- **Throughput** — orders/sec through the matching core.
- **Tail latency** — p50 / p99 / p99.9 per `submit`.

State clearly that these are **matching-core microbenchmarks**, not end-to-end numbers.
