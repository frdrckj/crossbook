//! Crossbook CLI — approve allowances, create/sign EIP-712 orders, submit them
//! to the engine, and query book/trade state.
//!
//! Milestone status: **M0 scaffold** — implemented in M5.

use anyhow::Result;

fn main() -> Result<()> {
    println!("crossbook-cli: M0 scaffold — client lands in M5");
    Ok(())
}
